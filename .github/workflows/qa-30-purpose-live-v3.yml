name: QA 30 client purpose relevance live v3

on:
  push:
    paths:
      - '.github/workflows/qa-30-purpose-live-v3.yml'

permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      BASE_URL: https://grantforgeusa-v11-backend.onrender.com
    steps:
      - uses: actions/checkout@v4
      - name: Reuse canonical 30-client live suite against v3
        run: |
          python - <<'PY'
          from pathlib import Path
          text = Path('.github/workflows/qa-30-purpose-live.yml').read_text()
          start_marker = "          python - <<'PY'\n"
          end_marker = "\n          PY\n"
          start = text.index(start_marker) + len(start_marker)
          end = text.index(end_marker, start)
          lines = text[start:end].splitlines()
          body = "\n".join(line[10:] if line.startswith("          ") else line for line in lines)
          Path('/tmp/qa30.py').write_text(body)
          PY
          python /tmp/qa30.py
      - name: Upload 30-client QA report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-30-purpose-report-v3
          path: qa-30-purpose-report.json
